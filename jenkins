pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build stage running 🚀'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing in progress ✅'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying application 🎯'
            }
        }
    }
}
